<%= @home_team.zip(@score, @avay_team).reverse %><br><br>

<%= @wins %><br><br>

<%= @only.reverse %>
<%= @sum %>

<%= form_tag("?", method: "get") do %>
  <%= text_field_tag(:q)%>
  <%= submit_tag "Поиск"%>
    <%end%>
